import { Box, styled } from "@mui/material";

export const StyleRoot  = styled(Box)(({theme})=> ({
    padding: theme.spacing(5),
    marginTop: theme.spacing(5),
    ml: 'auto'
}))
export const StyleBox  = styled(Box)(({theme})=> ({
    padding: theme.spacing(5),
    
}))