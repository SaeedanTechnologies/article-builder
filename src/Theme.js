import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#0077b6",
    },
    secondary: {
      main: "#3E3A57",
    },
    custom: {
      main: "#e2e2e2",
      gray: "#80808080",
    },
  },
});
